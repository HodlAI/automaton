<<REPLACE>>
    {
      name: "topup_credits",
      description:
        "Buy Conway compute credits by paying USDC from your wallet via x402. Valid tier amounts: $5, $25, $100, $500, $1000, $2500. Check your USDC balance first with check_usdc_balance.",
      category: "financial",
      riskLevel: "caution",
      parameters: {
<<WITH>>
    {
      name: "buy_web4ai_token",
      description:
        "Swap BNB for $WEB4AI tokens on PancakeSwap to increase your compute quota (Holding is Breathing). Calling this tool executes an on-chain DEX trade. Make sure you have enough BNB before calling.",
      category: "financial",
      riskLevel: "dangerous",
      parameters: {
        type: "object",
        properties: {
          amount_bnb: {
            type: "number",
            description: "Amount of BNB to spend on buying $WEB4AI",
          },
        },
        required: ["amount_bnb"],
      },
      execute: async (args, ctx) => {
        try {
          // Dynamic import of viem to avoid loading it for non-financial operations
          const { createPublicClient, createWalletClient, http, parseEther, formatEther } = await import("viem");
          const { privateKeyToAccount } = await import("viem/accounts");
          const { bsc } = await import("viem/chains");

          const amountBnb = args.amount_bnb as number;
          if (!amountBnb || amountBnb <= 0) {
            return "Invalid BNB amount specified.";
          }

          // PancakeSwap V2 Router: 0x10ED43C718714eb63d5aA57B78B54704E256024E
          // WEB4AI Token Address (Needs to be replaced with actual mainnet address)
          const ROUTER_ADDRESS = "0x10ED43C718714eb63d5aA57B78B54704E256024E";
          const WBNB_ADDRESS = "0xbb4CdB9CBd36B01bD1cBaEBF2De08d9173bc095c";
          const WEB4AI_ADDRESS = "0x987e6269c6b7ea6898221882f11ea16f87b97777"; // Placeholder / Replace when live

          const account = privateKeyToAccount(ctx.identity.privateKey as `0x${string}`);
          const publicClient = createPublicClient({ chain: bsc, transport: http() });
          const walletClient = createWalletClient({ account, chain: bsc, transport: http() });

          const balance = await publicClient.getBalance({ address: account.address });
          const valueToSend = parseEther(amountBnb.toString());

          // Leave some BNB for gas (assuming ~0.001 BNB for a swap)
          if (balance < valueToSend + parseEther("0.001")) {
             return `Insufficient BNB balance. You have ${formatEther(balance)} BNB, but requested to spend ${amountBnb} BNB. Leave some for gas.`;
          }

          // Build swapExactETHForTokens transaction payload
          const deadline = BigInt(Math.floor(Date.now() / 1000) + 60 * 20); // 20 mins
          
          const hash = await walletClient.writeContract({
            address: ROUTER_ADDRESS,
            abi: [{
              inputs: [
                { internalType: "uint256", name: "amountOutMin", type: "uint256" },
                { internalType: "address[]", name: "path", type: "address[]" },
                { internalType: "address", name: "to", type: "address" },
                { internalType: "uint256", name: "deadline", type: "uint256" }
              ],
              name: "swapExactETHForTokens",
              outputs: [{ internalType: "uint256[]", name: "amounts", type: "uint256[]" }],
              stateMutability: "payable",
              type: "function"
            }],
            functionName: "swapExactETHForTokens",
            args: [
              0n, // amountOutMin (set to 0 for simplicity, accept any slippage for now)
              [WBNB_ADDRESS, WEB4AI_ADDRESS], // path
              account.address, // to
              deadline, // deadline
            ],
            value: valueToSend,
          });

          return `Successfully initiated swap on PancakeSwap! TX Hash: ${hash}\nWait a few blocks for your $WEB4AI balance to reflect and your compute quota to increase.`;

        } catch (error: any) {
          return `Swap execution failed: ${error.message}`;
        }
      },
    },
    {
      name: "topup_credits",
      description:
        "Buy Conway compute credits by paying USDC from your wallet via x402. Valid tier amounts: $5, $25, $100, $500, $1000, $2500. Check your USDC balance first with check_usdc_balance.",
      category: "financial",
      riskLevel: "caution",
      parameters: {
<<END>>
